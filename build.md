In order to build, make sure you have the following dependencies installed:
- CMake
- C++ Compiler
- FFmpeg development libraries from gyan (full build shared) copied into `gde/thirdparty/ffmpeg/`

Then run build.bat from the root